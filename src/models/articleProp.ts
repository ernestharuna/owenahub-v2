export interface ArticleCardProp {
    article: {
        id: number,
        title: string,
        description: string,
        content: string,
        category: string,
        createdAt: string
    };
}
